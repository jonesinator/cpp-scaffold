# .vscode/

VS Code workspace configuration for local development and the dev container.

| File | Description |
|---|---|
| [launch.json](launch.json) | Debug launch configurations for `csv2json` and `json2csv` using GDB with pretty-printing |
| [tasks.json](tasks.json) | Build tasks using CMake presets (configure + build debug) |
