# ci/arch/

CI container for Arch Linux (latest). Matches the primary development environment.

| File | Description |
|---|---|
| [Dockerfile](Dockerfile) | Installs toolchain via pacman, copies source, runs `just check` |
